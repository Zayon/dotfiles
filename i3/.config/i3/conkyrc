background no
out_to_x no
out_to_console yes
update_interval 3
total_run_times 0
use_spacer none

TEXT
${exec php -f ~/.config/i3/song.php} | \
${if_match "${addr wlp2s0}" != "No Address"} : ${addr wlp2s0}${else} ${addr enp0s31f6}${endif} | \
 ${mem} | \
 ${battery_percent}% ${battery_time} | \
${time} | \
